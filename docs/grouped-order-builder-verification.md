# Grouped order builder verification

Verified on 2026-09-16. Build displays one product card containing editable flavor rows, Add flavors, per-row editing/removal and whole-product removal. Owners can open the catalog editor from Build and explicitly add, rename or remove variant rows with prices and barcodes. Catalog changes require Save product; accepted draft edits retain automatic saving.

- 436 Node 22 unit/API tests passed. New helper regressions cover stable grouping without mutation, exact product/flavor/unit/note quantity merging, original line IDs and metadata, atomic line/quantity limits, variant validation and retained price/barcode values. Existing submitted-order snapshots and server access checks remain unchanged.
- Four complete Chromium/WebKit flows at 390px and 1440px passed 23 checkpoints each. They cover grouping, edits, add/remove, estimates, distinct notes/units, unavailable flavors, Undo/Redo, online autosave and reload with exact saved IDs and values. Mobile and desktop screenshots were inspected for alignment and overflow.
- 32 focused Chromium/WebKit cases passed: 16 account/store/draft/permission races, eight invalid-input/unsaved-note/merge/new-draft cases, four quantity-blur click cases and four changed-case-size races. The reproduced first-click bug after editing a quantity is fixed by updating totals in place. Both the flavor picker and line editor reject a case-size change while their dialog is open.
- 84 catalog-editor browser assertions passed across Chromium and WebKit. They cover add/remove/rename, prices and barcodes, real synthetic domain saves, metadata preservation, rejected duplicates/reserved/blank names, cancel, pending-save controls, the 200-variant limit, version conflicts and photo-prefill/upload retry. Screens at 320px, 390px and 1440px were checked.
- Build and whitespace checks passed. The production dependency audit reported zero vulnerabilities. Independent implementation and browser reviewers reported no unresolved blockers.

All browser checks used synthetic accounts, catalog records and drafts in an isolated fixture. No production orders, catalog changes, emails or financial transactions were created by testing. Reports, runners and screenshots are under the ignored `.firebase/upgrade/grouped-builder/` and `.firebase/upgrade/catalog-variant-editor/` directories. GitHub CI also runs Firestore rule tests before merge. Release verification checks the App Hosting source commit and traffic, live asset hashes and unauthenticated API rejection.
